// Development configuration for authentication
// Set this to true to simulate an authenticated user, false for unauthenticated
export const MOCK_AUTHENTICATED_USER = false;

// Mock user data when authenticated
export const MOCK_USER_DATA = {
    id: "mock-user-123",
    email: "user@example.com",
    name: "John Doe"
};
